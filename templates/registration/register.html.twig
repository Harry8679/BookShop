{% extends 'base.html.twig' %}

{% block title %}Créer un compte — HolyBloom{% endblock %}

{% block body %}
<section class="min-vh-100 d-flex align-items-center justify-content-center bg-gradient">
  <div class="card shadow-lg border-0 rounded-4 p-4 p-md-5" style="max-width: 520px; width: 100%;">

    {# --- Titre --- #}
    <div class="text-center mb-4">
      <h1 class="fw-bold text-holybloom mb-2">
        <i class="bi bi-stars me-2"></i>
        Créer votre compte ✨
      </h1>
      <p class="text-secondary small">
        Rejoignez la communauté des femmes inspirées par la foi 💗
      </p>
    </div>

    {{ form_start(registrationForm, { attr: { class: 'needs-validation', novalidate: 'novalidate' } }) }}

      <div class="mb-3">
        {{ form_label(registrationForm.firstName, "Prénom", { label_attr: { class: 'form-label fw-semibold' } }) }}
        {{ form_widget(registrationForm.firstName, { attr: {
          class: 'form-control rounded-3 border-holybloom focus-ring',
          placeholder: 'Marie…'
        } }) }}
      </div>

      <div class="mb-3">
        {{ form_label(registrationForm.lastName, "Nom", { label_attr: { class: 'form-label fw-semibold' } }) }}
        {{ form_widget(registrationForm.lastName, { attr: {
          class: 'form-control rounded-3 border-holybloom focus-ring',
          placeholder: 'Dupont…'
        } }) }}
      </div>

      <div class="mb-3">
        {{ form_label(registrationForm.email, "Adresse e-mail", { label_attr: { class: 'form-label fw-semibold' } }) }}
        {{ form_widget(registrationForm.email, { attr: {
          class: 'form-control rounded-3 border-holybloom focus-ring',
          placeholder: 'exemple@gmail.com'
        } }) }}
      </div>

      <div class="mb-3">
        {{ form_label(registrationForm.plainPassword, "Mot de passe", { label_attr: { class: 'form-label fw-semibold' } }) }}
        {{ form_widget(registrationForm.plainPassword, { attr: {
          class: 'form-control rounded-3 border-holybloom focus-ring',
          placeholder: '••••••••'
        } }) }}
      </div>

      <div class="mb-3">
        {{ form_label(registrationForm.confirmPassword, "Confirmez le mot de passe", { label_attr: { class: 'form-label fw-semibold' } }) }}
        {{ form_widget(registrationForm.confirmPassword, { attr: {
          class: 'form-control rounded-3 border-holybloom focus-ring',
          placeholder: '••••••••'
        } }) }}
        <div class="form-text text-muted small">
          Les mots de passe doivent correspondre ✅
        </div>
      </div>

      <div class="d-grid mt-4">
        <button class="btn btn-holybloom py-2 fw-semibold rounded-3 shadow-sm">
          ✨ Créer mon compte
        </button>
      </div>

    {{ form_end(registrationForm) }}

    <p class="text-center text-muted small mt-4">
      Déjà un compte ?
      <a href="{{ path('app_login') }}" class="text-holybloom fw-semibold text-decoration-none">Se connecter</a>
    </p>
  </div>
</section>

<style>
  /* 🌸 Dégradé d’arrière-plan */
  .bg-gradient {
    background: linear-gradient(180deg, #fdf2f8 0%, #ffffff 60%, #f5e8ff 100%);
  }

  /* 💜 Couleurs HolyBloom */
  .text-holybloom {
    color: #b97add !important;
  }
  .border-holybloom {
    border-color: #e3c4f5 !important;
  }
  .btn-holybloom {
    background-color: #b97add;
    border: none;
    color: #fff;
    transition: all 0.3s ease;
  }
  .btn-holybloom:hover {
    background-color: #a25ed1;
    transform: translateY(-2px);
  }

  /* ✨ Focus et esthétique des champs */
  .focus-ring:focus {
    border-color: #b97add !important;
    box-shadow: 0 0 0 0.2rem rgba(185, 122, 221, 0.25) !important;
  }

  .card {
    border: 1px solid rgba(185, 122, 221, 0.1);
    background-color: #fff;
  }
</style>
{% endblock %}