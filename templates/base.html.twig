<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}HolyBloom{% endblock %}</title>

    {# 🌸 Bootstrap 5 CDN #}
    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
        rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
        crossorigin="anonymous"
    >

    {# 🌿 Font Awesome (CDN stable) #}
    <script src="https://kit.fontawesome.com/506909526a.js" crossorigin="anonymous"></script>

    {# (optionnel) Bootstrap Icons #}
    <link
        rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    >

    <style>
        body {
            background-color: #f8f9fa;
        }
        header {
            border-bottom: 1px solid #e5e5e5;
            background: white;
        }
        footer {
            background: #212529;
            color: #bbb;
        }
        a {
            text-decoration: none;
        }
    </style>
</head>

<body class="d-flex flex-column min-vh-100">

    {# 🔝 Barre promo #}
    <div class="bg-dark text-white text-center py-2 small">
        Livraison gratuite internationale pour les commandes de +40€
    </div>

    {# --- Header / Navbar --- #}
    <header>
        <div class="container py-3 d-flex justify-content-between align-items-center">

            {# Réseaux sociaux à gauche #}
            <div class="d-flex gap-3 text-dark fs-5">
                <a href="#" class="text-dark"><i class="fab fa-facebook-f"></i></a>
                <a href="#" class="text-dark"><i class="fab fa-twitter"></i></a>
                <a href="#" class="text-dark"><i class="fab fa-youtube"></i></a>
                <a href="#" class="text-dark"><i class="fab fa-instagram"></i></a>
            </div>

            {# Logo centré #}
            <a href="{{ path('app_home') }}" class="position-absolute start-50 translate-middle-x">
                <img src="{{ asset('logo.svg') }}" alt="HolyBloom Logo" height="40">
            </a>

            {# Liens utilisateur + panier #}
            <div class="d-flex align-items-center gap-3 text-dark">
                {% if app.user %}
                    <div class="dropdown">
                        <a class="btn btn-sm btn-light dropdown-toggle d-flex align-items-center gap-2" href="#" role="button" id="userDropdown"
                            data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-person-circle"></i>
                            <span>Bonjour, {{ app.user.firstName }}</span>
                        </a>

                        <ul class="dropdown-menu dropdown-menu-end shadow-sm" aria-labelledby="userDropdown">
                            <li>
                                {# <a class="dropdown-item" href="{{ path('app_user_profile') }}"> #}
                                <a class="dropdown-item" href="#">
                                    <i class="bi bi-person me-2"></i> Voir mon profil
                                </a>
                            </li>
                            <li>
                                {# <a class="dropdown-item" href="{{ path('app_user_edit') }}"> #}
                                <a class="dropdown-item" href="#">
                                    <i class="bi bi-pencil-square me-2"></i> Modifier mon profil
                                </a>
                            </li>
                            <li>
                                {# <a class="dropdown-item" href="{{ path('app_user_password') }}"> #}
                                <a class="dropdown-item" href="#">
                                    <i class="bi bi-lock me-2"></i> Modifier mon mot de passe
                                </a>
                            </li>
                            <li>
                                {# <a class="dropdown-item" href="{{ path('app_address_index') }}"> #}
                                <a class="dropdown-item" href="#">
                                    <i class="bi bi-geo-alt me-2"></i> Gérer mes adresses
                                </a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                                    <i class="bi bi-box-arrow-right me-2"></i> Déconnexion
                                </a>
                            </li>
                        </ul>
                    </div>
                {% else %}
                    <a href="{{ path('app_register') }}" class="text-decoration-none small text-secondary">Inscription</a> |
                    <a href="{{ path('app_login') }}" class="text-decoration-none small text-secondary">Connexion</a>
                {% endif %}


                {# <a href="{{ path('app_cart_index') }}" class="position-relative text-dark fs-5"> #}
                <a href="#" class="position-relative text-dark fs-5">
                    <i class="bi bi-cart3"></i>
                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-dark">0</span>
                </a>
            </div>
        </div>

        {# Menu secondaire #}
        <nav class="border-top">
            <ul class="nav justify-content-center py-3 fw-medium">
                <li class="nav-item"><a href="{{ path('app_home') }}" class="nav-link text-dark">Accueil</a></li>
                <li class="nav-item"><a href="{{ path('app_shop_index') }}" class="nav-link text-dark">Boutique</a></li>
                <li class="nav-item"><a href="#" class="nav-link text-dark">À propos</a></li>
                <li class="nav-item"><a href="#" class="nav-link text-dark">Voir mon panier</a></li>
                <li class="nav-item"><a href="#" class="nav-link text-dark">Contact</a></li>
            </ul>
        </nav>
    </header>

    {# --- Contenu principal --- #}
    <main class="grow container py-5">
        {% block body %}{% endblock %}
    </main>

    {# --- Footer --- #}
    <footer class="text-center py-4 mt-auto">
        <p class="small mb-0">
            &copy; {{ "now"|date("Y") }} <span class="text-light fw-bold">HolyBloom</span> — Tous droits réservés.
        </p>
    </footer>

    {# Bootstrap JS (nécessaire pour certains composants comme dropdown, modals, etc.) #}
    <script
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"
    ></script>
</body>
</html>