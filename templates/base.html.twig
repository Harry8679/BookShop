<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}HolyBloom{% endblock %}</title>

    {# 🌿 Mode DEV : charge d’abord le CSS statique, puis Vite pour le HMR #}
    {% if app.debug %}
        {# 1️⃣ On charge un CSS statique directement depuis /public pour éviter le flash #}
        <link rel="stylesheet" href="{{ asset('build/assets/app.css') }}">

        {# 2️⃣ Ensuite, on connecte Vite pour le hot reload JS/CSS #}
        <script type="module" src="http://localhost:5173/@vite/client"></script>
        <script type="module" src="http://localhost:5173/assets/app.js"></script>
    {% else %}
        {# 🌸 Mode PROD : tout est servi depuis le build #}
        <link rel="stylesheet" href="{{ asset('build/assets/app.css') }}">
        <script type="module" src="{{ asset('build/assets/app.js') }}"></script>
    {% endif %}
    {# <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" integrity="sha512-6RkO2yy0J+v6j5dy7uGb7XlFV8+gn9aI8ikKZ+WzCygkVZ7fKoV70Y7kMjGuYwX7lZjh4PfpwCwZBoqLqT6MFQ==" crossorigin="anonymous" referrerpolicy="no-referrer" /> #}
    <link
        rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
        integrity="sha512-SzlrxWUlpfuzQ+pcUCosxcglQRNAq/DZjVsC0lE40xsADsfeQoEypE+enwcOiGjk/bSuGGKHEyjSoQ1zVisanQ=="
        crossorigin="anonymous"
        referrerpolicy="no-referrer"
    />
</head>

<body class="bg-gray-100 text-gray-900 min-h-screen flex flex-col">
    {# 🔝 Barre promotionnelle #}
    <div class="bg-black text-white text-sm text-center py-2 tracking-wide">
        Livraison gratuite internationale pour les commandes de +40€
    </div>

    {# --- Header / Navbar --- #}
    <header class="border-b border-neutral-200 bg-white">
        <div class="container mx-auto flex justify-between items-center py-4 px-6">
            {# 🤝 Réseaux sociaux gauche #}
            <div class="flex gap-4 text-neutral-800">
                <a href="#" class="hover:text-blue-600 transition"><i class="fa-brands fa-facebook-f"></i></a>
                <a href="#" class="hover:text-sky-500 transition"><i class="fa-brands fa-twitter"></i></a>
                <a href="#" class="hover:text-red-600 transition"><i class="fa-brands fa-youtube"></i></a>
                <a href="#" class="hover:text-pink-500 transition"><i class="fa-brands fa-instagram"></i></a>
            </div>

            {# 🏛️ Logo au centre #}
            <a href="{{ path('app_home') }}" class="absolute left-1/2 transform -translate-x-1/2 flex items-center">
                <img src="{{ asset('logo.svg') }}" alt="HolyBloom Logo" class="h-10">
            </a>

            {# 🛒 Actions à droite #}
            <div class="flex items-center gap-6 text-neutral-800">
                {% if app.user %}
                    <span class="text-sm">👋 Bonjour, {{ app.user.firstName }}</span>
                    <a href="{{ path('app_logout') }}" class="hover:text-neutral-500 transition text-sm">Déconnexion</a>
                {% else %}
                    <a href="{{ path('app_register') }}" class="hover:text-neutral-500 transition text-sm">Inscription</a> |
                    <a href="{{ path('app_login') }}" class="hover:text-neutral-500 transition text-sm">Connexion</a>
                {% endif %}
                {# <a href="{{ path('app_register') }}" class="hover:text-neutral-500 transition text-sm">Inscription</a> |
                <a href="{{ path('app_login') }}" class="hover:text-neutral-500 transition text-sm">Connexion</a> #}

                <a href="#" class="hover:text-neutral-500 transition relative">
                    <i class="fa-solid fa-shopping-bag text-xl"></i>
                    <span class="absolute -top-2 -right-2 text-xs bg-black text-white rounded-full px-1">0</span>
                </a>
            </div>
        </div>

        {# 🔽 Menu secondaire sous le logo #}
        <nav class="bg-white border-t border-neutral-200">
            <ul class="flex justify-center gap-10 text-neutral-700 font-medium py-4 tracking-wide">
                <li><a href="{{ path('app_home') }}" class="hover:text-black transition">Accueil</a></li>
                <li><a href="{{ path('app_shop_index') }}" class="hover:text-black transition">Boutique</a></li>
                <li><a href="#" class="hover:text-black transition">À propos</a></li>
                <li><a href="#" class="hover:text-black transition">Voir mon panier</a></li>
                <li><a href="#" class="hover:text-black transition">Contact</a></li>
            </ul>
        </nav>
    </header>

    {# --- Contenu principal --- #}
    <main class="grow container mx-auto px-6 py-12">
        {% block body %}{% endblock %}
    </main>

    {# --- Footer --- #}
    <footer class="bg-gray-800 text-gray-300 text-center py-6 mt-auto">
        <p class="text-sm">
            &copy; {{ "now"|date("Y") }} <span class="text-blue-400 font-semibold">Bookshop</span> — Tous droits réservés.
        </p>
    </footer>
</body>
</html>